List of files that need changed for updated versions:
* Create new messages/update-vX.X.txt
* Include that file in messages.json
* Update README.md 'currentversion' url to new version (Ensure version matches repo tag)

* Commit the repo
* Tag the repo with the new version
* Push the repo and the tag to github